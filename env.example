# Required for AI schema generation (server-side only — never commit real keys)
OPENAI_API_KEY=sk-...

# Optional — default gpt-4o-mini
# OPENAI_MODEL=gpt-4o-mini

# Port for the local API + static UI
# PORT=3000
